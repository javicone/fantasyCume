# --- Configuraci?n de MySQL (Docker) para TESTS ---

# Conexi?n a tu Docker
spring.datasource.url=jdbc:mysql://localhost:3306/fantasyCume
spring.datasource.username=admin
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dialecto de MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ¡El más importante! Borra y crea la BBDD cada vez que se ejecuten los tests.
spring.jpa.hibernate.ddl-auto=create

# --- Perfil de Desarrollo ---
# Descomentar la siguiente l?nea para activar el script de inicializaci?n de datos
# Este script poblar? autom?ticamente la base de datos al iniciar la aplicaci?n
# SOLO se ejecuta si la base de datos est? vac?a
# Para m?s informaci?n, ver INICIALIZACION_BBDD.md
spring.profiles.active=dev

# --- Configuraci?n de Thymeleaf ---
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

# --- Configuraci?n de OpenRouter AI ---
# API URL: https://openrouter.ai/api/v1/chat/completions
# Modelo: Nemotron (nvidia/llama-3.1-nemotron-70b-instruct)
openrouter.api.key=${OPENROUTER_API_KEY}
openrouter.api.url=https://openrouter.ai/api/v1/chat/completions

#ALTERNATIVA SI EL BUENO DA PROBLEMAS
openrouter.model=meta-llama/llama-3.2-3b-instruct:free 
# ESTE ES EL BUENO
#openrouter.model=nvidia/nemotron-3-nano-30b-a3b:free
# ESTE ES DE PAGO
#openrouter.model=nvidia/llama-3.1-nemotron-70b-instruct


